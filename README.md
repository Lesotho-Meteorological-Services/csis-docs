# CSIS Documentation

This repository contains the core documentation for the Climate Services Information System (CSIS).

## Local development

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
mkdocs serve
```

## Publishing

This site is published to GitHub Pages from the `main` branch through GitHub Actions.
